plugins {
    id 'java-library'
}

dependencies {
    implementation 'jakarta.persistence:jakarta.persistence-api' // 단순 엔티티 작성
    compileOnly 'org.springframework.data:spring-data-jpa'
}

// 다른 모듈이 쓸 수 있게 일반 jar로 생성
jar { enabled = true }